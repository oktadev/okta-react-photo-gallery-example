version: '2'
services:
    gallery-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/gallery/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=gallery
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
